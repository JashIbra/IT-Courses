<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/main_fragment">

    <fragment
        android:id="@+id/main_fragment"
        android:name="com.effectivemobilett.presentation.main.MainFragment"
        android:label="@string/main_fragment_label"
        tools:layout="@layout/fragment_main" />

    <fragment
        android:id="@+id/save_fragment"
        android:name="com.effectivemobilett.presentation.save.SaveFragment"
        android:label="@string/save_fragment_label"
        tools:layout="@layout/fragment_save" />

    <fragment
        android:id="@+id/account_fragment"
        android:name="com.effectivemobilett.presentation.account.AccountFragment"
        android:label="@string/account_fragment_label"
        tools:layout="@layout/fragment_account" />

</navigation>